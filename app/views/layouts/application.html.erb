<!DOCTYPE html>
<html>
<head>
  <title>Funbetz</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <% flash.each do |name, msg| %>
    <% content_tag :div, msg, :id => "flash_#{name}" %>
  <% end %>
  <nav>
    <ul>
      <li class"nav"><%= link_to 'Home', home_path %></li>
      <li class"nav"><%= link_to 'About', about_path %></li>
      <li class"nav">Forum</li>
      <li class"nav">Top-Players</li>
      <li class"nav"><%= link_to 'My-Cards', cards_path %></li>
      <% if current_user %>
      <li class"nav">Logged in as <%= current_user.username %></li>
      <li class"nav"><%= link_to "Log out", log_out_path %></li>
      <% else %>
      <li class"nav"><%= link_to "Sign-In", sign_in_path %> or</li>
      <li class"nav"><%= link_to "Sign-Up", sign_up_path %></li>
      <% end %>
    </ul>
  </nav>

<%= yield %>

</body>
</html>
